Ear landmark live — share pack

Desktop:
  python -m venv .venv && source .venv/bin/activate
  pip install -r requirements.txt
  python app.py

Browser:
  cd web && npm install && npm start
  open http://127.0.0.1:8765
